mplement a multiplayer version of Conway's Game of Life: https://en.wikipedia.org/wiki/Conway%27s_Game_of_Life

 

The game should run one instance and multiple clients should be able to connect to it and observe the generation updates. Pick the communication protocol of your choice.



Support a large universe (e.g. 2^64 x 2^64). 
The universe should wrap on itself in both dimensions (think of a torus). The universe is mostly empty, except for the area with object(s) -- for example, Gosper's glider gun in the center.



There should be a simple UI with the possibility to interactively configure the initial state of the universe (100 x 100), start the algorithm, and observe the changes. The UI can be console based.



There should also be a function to store/load the state. Please prepare an example file with Gosper's glider gun.



You are expected to use AI agents for the task. Be prepared to discuss your approach and work process.



wrapvane kato stigne 2^64 nqkoq kletka

save/load
    dali samo purvonachalen state ili moje da save i running?
        shte probvam da implementiram running ne zvuchi po trudno, ako se okaje fallback samo purvonachalno


Refactor TUI:
    move lock inside model
    see if we really need TuiScreen?!?